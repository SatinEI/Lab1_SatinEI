<Window x:Class="Lab1_SatinEI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab1_SatinEI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,5">
            <Label Width="60">Сторона A:</Label>
            <TextBox x:Name="txtSideA" Width="200"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <Label Width="60">Сторона B:</Label>
            <TextBox x:Name="txtSideB" Width="200"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5">
            <Label Width="60">Сторона C:</Label>
            <TextBox x:Name="txtSideC" Width="200"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Вычислить" Width="100" Click="Button_Click" Margin="0,10"/>

        <StackPanel Grid.Row="4" Margin="0,5">
            <Label x:Name="lblType" FontWeight="Bold"/>
            <Label x:Name="lblVertices"/>
        </StackPanel>

        <!-- Поле для отрисовки треугольника -->
        <Canvas Grid.Row="5" Width="100" Height="100" Background="LightGray" Margin="0,10">
            <Polygon x:Name="trianglePolygon" Stroke="Blue" StrokeThickness="2" Fill="LightBlue"/>
        </Canvas>
    </Grid>
</Window>
